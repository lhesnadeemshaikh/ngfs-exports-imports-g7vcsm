/**
 * https://stackblitz.com/edit/blastoff-imports
 * Exercise
 * 1. Export the `User` interface in the 'types.ts' file
 * 2. Import the User type from the 'types.ts' file
 * 3. Create an array of users
 * 4. Export a new Donut interface in the 'types.ts' file, that has 
 *    at least a name property, and any other property you want
 * 5. Import the Donut type and create an array of your favorite Donuts
 * 6. Extra Credit: Create an interface with a property of with a type of 
 *    another interface that you created
 */
